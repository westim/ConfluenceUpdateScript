# Confluence Updating Script

This is a simple script used to update pages on Confluence.

## Using the Script

1. Update `BASE_URL` and `VIEW_URL` for your needs.
2. Run `python ConfluenceAccess.py -h` for all available commands.
